# Claude Code OAuth Token (Required)
# Get this from your Claude Code subscription
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-...

# Cloudflare Account (Required for wrangler CLI)
# Find this in Cloudflare Dashboard after running: wrangler login
CLOUDFLARE_ACCOUNT_ID=your_account_id

# Scout Worker URL (Required - deployed worker)
# After deploying with: wrangler deploy --config infra/cloudflare/wrangler.toml
SCOUT_WORKER_URL=https://kanousei-scout.your-subdomain.workers.dev

# Target URL (Required - website to clone)
TARGET_URL=https://example.com

# Optional: Vercel deployment (if you want to deploy the generated Next.js site)
VERCEL_TOKEN=your_vercel_token
VERCEL_PROJECT=your-project-name
